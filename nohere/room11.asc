// room script file

function room_AfterFadeIn()
{
  oload.SetView(15, 1, 1);
  oload.Animate(0, 10, eOnce, eBlock);
  //oload.Visible=false;
  SetBackgroundFrame(1);
  deye.Start();
  if (!oload.Animating)
  {
    oload.Visible = false;
    player.ChangeRoom(16);
  }


}
